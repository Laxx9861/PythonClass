# Defining Class
class Mic:
    def __init__(self, name, price):
        self.name = name
        self.price = price

    


# Defining Object
mic = Mic("Rode", 450000)
print(mic.name)
print(mic.price)

